#include<stdio.h>

/**
 * main - print numbers base 16
 *
 * Return: always 0
 */
int main(void)
{
	int g;

	for (g = 10; g < 20; g++)
	{
		putchar(g);
	}
	for (g = 22; g < 28; g++)
	{
		putchar(g);
	}
	putchar('\n');
	return (0);
}
