#include "main.h"

/**
 * _isupper - checks uppercase character
 * @c: tye letter
 * Return: 1 if c is uppercase, 0 otherwise
 */
int _isupper(int c)
{
	var c = 'A'
		
	if (/[A-Z]/.test(c))
	{
		alert('it is uppercase');
	}
	
	return (c);
}
