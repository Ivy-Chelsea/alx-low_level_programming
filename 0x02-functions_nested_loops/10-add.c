#include "main.h"
#include <stdio.h>

/**
 * add - prints result
 *
 * @q: type number
 * @x: type number
 *
 * Return: sum of q and x
 */
int add(int q, int x)
{
	int t;

	t = q + x;

	return (t);
}
